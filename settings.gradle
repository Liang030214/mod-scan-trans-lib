pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

plugins {
    // 该插件允许 Gradle 自动下载所需的 Java 版本(项目需要 Java 21)
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}
